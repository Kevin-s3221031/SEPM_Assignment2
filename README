Please ensure that you have the JDK and JRE installed.

1. In the command prompt navigate to the folder containing the ticketSystem.java file
2. Run the following command to compile: javac ticketSystem.java
3. Run the following command to execute the program: java ticketSystem
